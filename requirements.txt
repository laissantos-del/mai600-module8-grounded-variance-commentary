# Grounded Variance Commentary: direct dependencies only.
#
# This file previously held a 109-package `pip freeze` of the author's macOS
# environment. That could not install anywhere else, because it pinned every
# transitive package exactly and included `appnope`, which is macOS only. The
# assignment requires the project to run on another machine, so only the packages
# the code actually imports are listed here, with the versions it was tested
# against as lower bounds. pip resolves the rest for the target platform.
#
# Tested on Python 3.12.10, macOS 26.5, Apple silicon.
# Python 3.10 or newer is required.

# Data and numerics
pandas>=2.2
numpy>=1.26
matplotlib>=3.8

# Retrieval
sentence-transformers>=3.0        # pulls torch and transformers
faiss-cpu>=1.8                    # imported as `faiss`

# Talking to the local model server
requests>=2.31

# Review interface
streamlit>=1.36
altair>=5.0

# Running the notebook
nbformat>=5.10
nbconvert>=7.16
ipykernel>=6.29

# NOT a pip package. Generation needs Ollama installed separately, with the model
# pulled, and the server running:
#     https://ollama.com          then:  ollama pull qwen3:4b-instruct
